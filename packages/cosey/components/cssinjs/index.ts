import Keyframes from './Keyframes';

export type { DerivativeFunc } from './interface';

export type {
  CSSProperties,
  CSSPropertiesWithMultiValues,
  CSSPseudos,
  ArrayCSSInterpolation,
  InterpolationPrimitive,
  CSSInterpolation,
  CSSOthersObject,
  CSSObject,
  HashPriority,
  ParseConfig,
  ParseInfo,
} from './parser';

export { parseStyle } from './parser';

export { Keyframes };
